# Dictionary licences

`en-GB.aff` / `en-GB.dic` are copied from `dictionary-en-gb@3.0.0`.
`en.aff` / `en.dic` are copied from `dictionary-en@4.0.0`.

The npm wrappers are MIT. The affix and wordlist files remain
`(MIT AND BSD)` SCOWL / wordlist.aspell.net data. Full notices are in
`infra/proof/dependencies.md` and the installed package `license` files.

nspell@2.1.5 is MIT. It is not an API: dictionaries are processed on-device.
